import MainMenu from '../components/menus/MainMenu';

export const menuModes = {
    home: [
        { slug:"home", label:"BreatheCode", component: MainMenu }
    ],
    course: [
        { slug:"search", label:"Search" }
    ]
};